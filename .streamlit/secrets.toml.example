# Copy this file to secrets.toml and fill in your Railway PostgreSQL URL.
# Find it in Railway dashboard -> your PostgreSQL plugin -> Variables -> DATABASE_URL
DATABASE_URL = "postgresql://postgres:YOUR_PASSWORD@YOUR_HOST:YOUR_PORT/railway"
